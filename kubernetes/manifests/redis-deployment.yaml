apiVersion: apps/v1
kind: Deployment
metadata:
  name: climate-redis
  labels:
    app: climate-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: climate-redis
  template:
    metadata:
      labels:
        app: climate-redis
    spec:
      containers:
      - name: redis
        image: redis:alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: climate-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
