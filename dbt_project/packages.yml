packages:
  # dbt_utils — generic test yardımcıları (surrogate_key, expression_is_true vb.)
  - package: dbt-labs/dbt_utils
    version: [">=1.2.0", "<2.0.0"]

  # codegen — otomatik model/source YAML üretimi (geliştirme kolaylığı)
  - package: dbt-labs/codegen
    version: [">=0.12.0", "<1.0.0"]
